[
  {
    "id": "nodejs-001",
    "category": "nodejs",
    "difficulty": "mid",
    "type": "multiple-choice",
    "question": "Node.js의 이벤트 루프가 싱글 스레드임에도 높은 동시성을 처리할 수 있는 원리는?",
    "options": [
      "내부적으로 멀티스레드를 사용하기 때문에",
      "비동기 I/O를 OS 커널(epoll/kqueue)에 위임하고, 완료 시 콜백을 실행하는 논블로킹 모델",
      "요청마다 새로운 프로세스를 생성하기 때문에",
      "JIT 컴파일이 매우 빠르기 때문에"
    ],
    "answer": 1,
    "explanation": "Node.js는 I/O 작업(파일 읽기, 네트워크, DB 쿼리)을 OS 커널이나 libuv의 스레드 풀에 위임하고, 메인 스레드는 콜백 큐에서 완료된 작업을 처리한다. CPU가 아닌 I/O에 대기하는 시간이 대부분인 웹 서버에 최적화된 모델이다.",
    "seniorTip": "이벤트 루프의 6단계: timers → pending callbacks → idle/prepare → poll → check → close callbacks. setImmediate()는 check 단계, setTimeout(fn, 0)은 timers 단계에서 실행된다. process.nextTick()은 어떤 단계와 단계 사이에 실행되므로 I/O starvation을 일으킬 수 있다. libuv의 스레드 풀(기본 4개)은 DNS 조회, 파일 시스템, crypto 등 OS가 비동기를 지원하지 않는 작업에 사용된다. UV_THREADPOOL_SIZE로 조정 가능.",
    "tags": [
      "event-loop",
      "non-blocking",
      "libuv",
      "concurrency"
    ],
    "deepDive": ""
  },
  {
    "id": "nodejs-002",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "short-answer",
    "question": "Node.js에서 CPU 집약적인 작업(이미지 처리, 대용량 JSON 파싱 등)을 처리하면 왜 문제가 되고, 어떻게 해결하는가?",
    "answer": "이벤트 루프를 블로킹하여 모든 요청 처리가 멈춤. Worker Threads, 별도 프로세스, 외부 서비스로 위임하여 해결",
    "explanation": "CPU 작업은 이벤트 루프를 점유하므로, 그동안 다른 모든 요청이 대기한다. 100ms 이상 이벤트 루프를 점유하면 체감 성능이 급격히 나빠진다.",
    "seniorTip": "해결 방법 우선순위: 1) Worker Threads (Node.js 12+) — CPU 작업을 별도 스레드에서 실행. 2) Child Process (fork) — 별도 프로세스로 분리. 3) 큐(Bull/BullMQ + Redis) — 무거운 작업을 비동기 작업 큐로 분리. 이것이 가장 실무적. 4) 외부 서비스로 위임 (이미지 처리 → Sharp + 별도 마이크로서비스). 실무에서 가장 흔한 실수: JSON.parse()에 수MB의 데이터를 넣어서 이벤트 루프를 수백ms 블로킹하는 것. 스트리밍 JSON 파서(jsonstream)를 사용하자.",
    "tags": [
      "cpu-bound",
      "worker-threads",
      "event-loop-blocking",
      "bull"
    ],
    "deepDive": ""
  },
  {
    "id": "nodejs-003",
    "category": "nodejs",
    "difficulty": "mid",
    "type": "multiple-choice",
    "question": "Node.js에서 메모리 누수(Memory Leak)의 가장 흔한 원인은?",
    "options": [
      "let 대신 const를 사용해서",
      "클로저, 이벤트 리스너 미해제, 전역 변수에 데이터 축적",
      "async/await 사용",
      "npm 패키지가 너무 많아서"
    ],
    "answer": 1,
    "explanation": "1) 클로저가 외부 변수를 참조하여 GC가 해제하지 못함. 2) EventEmitter에서 리스너를 등록만 하고 removeListener하지 않음. 3) 전역 변수나 모듈 스코프 변수에 데이터를 계속 push. 4) 캐시를 Map/Object에 무한정 저장.",
    "seniorTip": "메모리 누수 디버깅: 1) --inspect 플래그로 Chrome DevTools 연결 → Heap Snapshot 비교. 2) process.memoryUsage()로 heapUsed 모니터링. 3) clinic.js로 프로파일링. K8s에서는 memory limits에 의해 OOM Kill → 재시작이 반복되면 메모리 누수를 의심. NestJS에서 흔한 패턴: @Injectable() 서비스에 인스턴스 변수로 데이터를 축적하는 것 (싱글톤이므로 서버 생존 기간 동안 계속 증가).",
    "tags": [
      "memory-leak",
      "garbage-collection",
      "debugging",
      "heap"
    ],
    "deepDive": ""
  },
  {
    "id": "nodejs-004",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "short-answer",
    "question": "Node.js의 Cluster 모듈과 PM2의 cluster mode가 하는 역할은? K8s 환경에서도 필요한가?",
    "answer": "멀티코어 활용을 위해 여러 워커 프로세스를 생성. K8s에서는 Pod replica로 대체하므로 불필요한 경우가 많다",
    "explanation": "Node.js는 싱글 스레드이므로 하나의 CPU 코어만 사용한다. Cluster/PM2는 코어 수만큼 워커 프로세스를 fork하여 부하를 분산한다. 하지만 K8s에서는 Pod를 여러 개 띄우면 동일한 효과를 얻을 수 있다.",
    "seniorTip": "K8s + Node.js 조합에서의 판단: 1) Pod 1개에 CPU를 1코어만 할당하고 replica를 늘리는 것이 K8s 친화적 (HPA가 Pod 단위로 스케일링). 2) Pod 1개에 4코어 + cluster mode 4 워커는 리소스 효율은 좋지만 스케일링 단위가 굵어짐. 3) 일반적으로 전자가 권장. 4) PM2는 K8s 환경에서 불필요 — K8s 자체가 프로세스 관리자 역할. 컨테이너 내에서는 node 직접 실행이 낫다 (PM2가 SIGTERM을 가로채면 graceful shutdown이 꼬일 수 있음).",
    "tags": [
      "cluster",
      "pm2",
      "multi-core",
      "kubernetes",
      "scaling"
    ],
    "deepDive": ""
  },
  {
    "id": "nodejs-005",
    "category": "nodejs",
    "difficulty": "mid",
    "type": "ox",
    "question": "Promise.all()에서 하나의 Promise가 reject되면, 나머지 Promise들은 자동으로 취소된다.",
    "answer": "X",
    "explanation": "Promise.all()이 reject되더라도 나머지 Promise는 계속 실행된다. JavaScript에는 Promise 취소 메커니즘이 없다. 이미 실행 중인 비동기 작업은 완료될 때까지 실행되지만, 결과가 무시될 뿐이다.",
    "seniorTip": "Promise.allSettled()를 사용하면 모든 Promise의 결과(fulfilled/rejected)를 받을 수 있다. 부분 실패를 허용하는 경우에 유용. AbortController와 AbortSignal을 사용하면 fetch나 다른 비동기 작업을 실제로 취소할 수 있다. Promise.race()는 가장 먼저 완료되는 것만 반환하지만 나머지도 실행은 계속됨. Promise.any()는 가장 먼저 성공하는 것을 반환.",
    "tags": [
      "promise",
      "async",
      "error-handling",
      "abort-controller"
    ],
    "deepDive": ""
  },
  {
    "id": "nodejs-006",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "multiple-choice",
    "question": "Node.js의 Stream이 대용량 파일 처리에서 중요한 이유는?",
    "options": [
      "파일을 압축하여 읽기 때문에",
      "파일 전체를 메모리에 올리지 않고 chunk 단위로 처리하여 메모리 사용량을 일정하게 유지",
      "멀티스레드로 파일을 분할 읽기하기 때문에",
      "디스크 캐시를 활용하기 때문에"
    ],
    "answer": 1,
    "explanation": "fs.readFile()은 파일 전체를 메모리에 로드한다. 1GB 파일이면 1GB 메모리 필요. Stream은 64KB chunk씩 읽으면서 처리하므로, 파일 크기와 관계없이 메모리 사용량이 일정하다.",
    "seniorTip": "Stream 종류: Readable(읽기), Writable(쓰기), Duplex(양방향), Transform(변환). pipe()로 연결: readStream.pipe(transformStream).pipe(writeStream). NestJS에서 파일 업로드 시 multer의 storage를 커스터마이징하여 S3에 직접 스트리밍하면, 서버 메모리를 거의 사용하지 않고 대용량 파일을 처리할 수 있다. 스트리밍 JSON 응답(StreamableFile)도 NestJS에서 지원.",
    "tags": [
      "stream",
      "memory",
      "file-processing",
      "backpressure"
    ],
    "deepDive": ""
  },
  {
    "id": "nodejs-007",
    "category": "nodejs",
    "difficulty": "mid",
    "type": "short-answer",
    "question": "package.json의 dependencies와 devDependencies의 차이를 설명하고, 프로덕션 빌드에 미치는 영향을 설명하라.",
    "answer": "dependencies는 런타임에 필요한 패키지, devDependencies는 개발/빌드 시에만 필요한 패키지. npm install --production은 devDependencies를 설치하지 않음",
    "explanation": "dependencies: 앱 실행에 필요한 패키지 (express, typeorm 등). devDependencies: 개발/테스트/빌드에만 필요 (jest, eslint, typescript 등). Docker 이미지에서 npm install --production으로 devDependencies를 제외하면 이미지 크기가 줄어든다.",
    "seniorTip": "Docker 멀티스테이지 빌드 패턴: 1) build 스테이지: npm install (devDeps 포함) → npm run build. 2) production 스테이지: npm install --production (devDeps 제외) → build 결과물 복사. 이렇게 하면 TypeScript 컴파일러, 테스트 라이브러리 등이 최종 이미지에 포함되지 않아 보안과 크기 모두 개선. package-lock.json도 반드시 커밋하고 npm ci를 사용하자 (결정적 설치).",
    "tags": [
      "npm",
      "dependencies",
      "docker",
      "multi-stage-build"
    ],
    "deepDive": ""
  },
  {
    "id": "nodejs-008",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "multiple-choice",
    "question": "V8 엔진의 가비지 컬렉션에서 Young Generation과 Old Generation의 차이는?",
    "options": [
      "Young은 큰 객체, Old는 작은 객체를 저장한다",
      "Young은 짧은 수명의 객체로 Minor GC(Scavenge), Old는 오래 생존한 객체로 Major GC(Mark-Sweep-Compact)",
      "Young은 동기, Old는 비동기로 수집한다",
      "차이 없이 모든 객체를 동일하게 처리한다"
    ],
    "answer": 1,
    "explanation": "V8은 Generational GC를 사용. 대부분의 객체는 금방 죽으므로(generational hypothesis), 새 객체는 Young Generation(Semi-space)에 할당하고 빠른 Scavenge GC로 수거. 2번 이상 살아남으면 Old Generation으로 이동하여 Mark-Sweep-Compact로 수거.",
    "seniorTip": "Node.js 메모리 튜닝: --max-old-space-size로 Old Generation 크기 설정 (기본 약 1.5GB). K8s에서 Pod memory limits와 맞춰야 OOM Kill을 방지. --max-semi-space-size로 Young Generation 크기 조정 가능 (처리량 증가, GC 빈도 감소). V8의 Orinoco GC(증분, 병렬, 동시)가 Stop-the-world 시간을 최소화하지만, 대용량 힙에서는 여전히 GC pause가 레이턴시에 영향을 줄 수 있다.",
    "tags": [
      "v8",
      "garbage-collection",
      "memory",
      "performance-tuning"
    ],
    "deepDive": ""
  },
  {
    "id": "nodejs-009",
    "category": "nodejs",
    "difficulty": "mid",
    "type": "ox",
    "question": "Node.js에서 require()는 모듈을 매번 새로 로드하고 실행한다.",
    "answer": "X",
    "explanation": "require()는 첫 번째 호출 시 모듈을 로드하고 실행한 후 결과를 캐시한다 (require.cache). 이후 같은 모듈을 require하면 캐시된 결과를 반환한다. 이것이 Node.js 모듈이 기본적으로 싱글톤처럼 동작하는 이유다.",
    "seniorTip": "모듈 캐싱의 함정: 1) 테스트에서 모듈의 상태를 리셋하려면 delete require.cache[require.resolve('./module')]이 필요할 수 있음. 2) jest에서 jest.resetModules()로 모듈 캐시를 초기화. 3) ESM의 import는 항상 같은 바인딩을 참조 (live binding). 4) NestJS의 DI가 모듈 시스템 위에서 한 번 더 싱글톤을 보장하므로, NestJS에서는 직접 require 캐싱을 걱정할 필요는 적다.",
    "tags": [
      "module",
      "require",
      "cache",
      "singleton",
      "esm"
    ],
    "deepDive": ""
  },
  {
    "id": "nodejs-010",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "short-answer",
    "question": "Node.js 애플리케이션의 에러 핸들링 전략을 운영 관점에서 설명하라. unhandledRejection, uncaughtException을 어떻게 다뤄야 하는가?",
    "answer": "uncaughtException은 프로세스 종료 전 로깅 후 종료. unhandledRejection은 Node 15+에서 프로세스를 종료시킴. 모든 Promise에 catch 필수",
    "explanation": "uncaughtException: 동기 에러가 catch되지 않으면 발생. 프로세스 상태가 불확실하므로 로깅 후 종료가 권장. unhandledRejection: Promise reject가 catch되지 않으면 발생. Node 15+부터 프로세스를 종료시킨다.",
    "seniorTip": "운영 전략: 1) process.on('unhandledRejection')에서 로그를 남기고 process.exit(1). 2) K8s가 재시작해줌 (restartPolicy: Always). 3) NestJS에서는 ExceptionFilter가 HTTP 요청 내 에러를 처리하지만, 비동기 작업(cron, queue worker)의 에러는 직접 try-catch 필요. 4) 글로벌 에러 로깅: Sentry, DataDog 등에 에러를 전송. 5) 에러 발생 시 graceful shutdown으로 진행 중인 요청 완료 후 종료. 절대 uncaughtException에서 process를 살려두려고 하지 말자 — 메모리/상태 오염 위험.",
    "tags": [
      "error-handling",
      "unhandled-rejection",
      "uncaught-exception",
      "operations"
    ],
    "deepDive": ""
  },
  {
    "id": "nodejs-011",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "short-answer",
    "question": "이벤트 루프의 각 단계(timers, poll, check)를 설명하고, setImmediate와 setTimeout(fn, 0)의 실행 순서가 달라지는 조건을 설명하라.",
    "answer": "timers(setTimeout/setInterval) → pending callbacks → idle/prepare → poll(I/O 대기) → check(setImmediate) → close callbacks 순서. I/O 콜백 내부에서는 setImmediate가 항상 먼저 실행되고, 최상위 컨텍스트에서는 순서가 비결정적",
    "explanation": "이벤트 루프는 6단계로 구성된다. timers 단계는 만료된 setTimeout/setInterval 콜백을 실행하고, poll 단계는 새 I/O 이벤트를 가져오며 콜백을 실행한다. check 단계는 setImmediate 콜백 전용이다. 최상위 레벨에서 setTimeout(fn, 0)과 setImmediate를 동시에 호출하면 타이머 정밀도에 따라 순서가 달라지지만, I/O 콜백 안에서는 check 단계가 timers보다 먼저 오므로 setImmediate가 항상 앞선다.",
    "seniorTip": "process.nextTick()은 이벤트 루프의 어떤 단계도 아니고, 현재 작업 완료 직후 다음 단계 진입 전에 실행된다(microtask queue). Promise의 .then()도 microtask queue에 쌓이며 nextTick보다는 늦게 실행된다. 실행 우선순위: process.nextTick > Promise.then > setImmediate > setTimeout. nextTick을 재귀 호출하면 I/O starvation이 발생할 수 있어 setImmediate를 쓰는 게 안전하다. 실제 IoT/소켓 서버에서 고빈도 이벤트 처리 시 이 우선순위가 레이턴시에 직접 영향을 준다.",
    "deepDive": "poll 단계에서 대기 시간은 어떻게 결정되는가? process.nextTick을 재귀로 쓰면 어떤 문제가 생기는가? queueMicrotask()와 process.nextTick()의 차이는?",
    "tags": [
      "event-loop",
      "setImmediate",
      "setTimeout",
      "nextTick",
      "libuv"
    ]
  },
  {
    "id": "nodejs-012",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "short-answer",
    "question": "libuv의 스레드 풀이 무엇인지, 어떤 작업이 스레드 풀을 사용하는지, UV_THREADPOOL_SIZE를 어떻게 결정해야 하는지 설명하라.",
    "answer": "libuv는 OS가 비동기를 지원하지 않는 작업(파일 I/O, DNS, crypto 등)을 위해 기본 4개의 스레드 풀을 제공. UV_THREADPOOL_SIZE로 최대 1024까지 조정 가능하며, 동시 처리 작업 수와 CPU 코어 수를 고려해 설정",
    "explanation": "네트워크 소켓은 OS 커널(epoll/kqueue)이 직접 비동기를 지원하지만, 파일 시스템과 DNS lookup, crypto(pbkdf2, scrypt, randomBytes) 등은 libuv가 스레드 풀로 처리한다. 스레드 풀 크기가 4인데 동시에 5개의 파일 읽기가 발생하면 4개는 즉시 시작하고 1개는 스레드가 해제될 때까지 대기한다.",
    "seniorTip": "AWS Lambda나 K8s Pod에서 crypto.pbkdf2()를 다수 병렬 처리하는 경우, UV_THREADPOOL_SIZE=사용 가능한 vCPU 수 정도로 조정하면 처리량이 개선된다. 단순히 무작정 올리면 컨텍스트 스위칭 비용이 증가하므로 벤치마크가 필수. Node.js 22부터 파일 I/O 일부가 io_uring(리눅스)을 사용해 스레드 풀 의존도가 줄어드는 방향으로 진화 중. Lambda 환경에서 cold start 문제의 한 원인이 스레드 풀 초기화임을 기억하자.",
    "deepDive": "스레드 풀을 사용하는 작업과 사용하지 않는 작업을 각각 3개씩 나열하라 UV_THREADPOOL_SIZE를 너무 크게 설정하면 어떤 부작용이 생기는가? io_uring이 libuv 스레드 풀을 어떻게 대체하는가?",
    "tags": [
      "libuv",
      "thread-pool",
      "UV_THREADPOOL_SIZE",
      "crypto",
      "file-io"
    ]
  },
  {
    "id": "nodejs-013",
    "category": "nodejs",
    "difficulty": "mid",
    "type": "multiple-choice",
    "question": "Node.js의 Buffer와 TypedArray(Uint8Array)의 관계로 올바른 것은?",
    "options": [
      "Buffer와 TypedArray는 완전히 별개의 독립적인 구현이다",
      "Buffer는 Uint8Array를 상속(extends)하므로 TypedArray API를 모두 사용할 수 있다",
      "TypedArray가 Buffer를 상속한다",
      "Buffer는 문자열만 다루고 TypedArray는 숫자만 다룬다"
    ],
    "answer": 1,
    "explanation": "Node.js 4.0부터 Buffer는 Uint8Array의 서브클래스다. 따라서 Buffer.alloc()으로 만든 버퍼는 forEach, slice, indexOf 등 TypedArray의 모든 메서드를 사용할 수 있다. 반대로 Uint8Array를 Buffer.from(uint8array)로 변환하면 동일한 메모리를 공유하는 Buffer를 얻는다.",
    "seniorTip": "바이너리 데이터 처리 실무 팁: 1) Buffer.alloc(size)는 0으로 초기화(안전), Buffer.allocUnsafe(size)는 초기화 안 함(빠름). 민감한 데이터가 담길 수 있는 경우 alloc 사용. 2) MQTT/WebSocket으로 받은 바이너리 프레임을 파싱할 때 Buffer.readUInt16BE(), readFloatLE() 등으로 바이트 단위 제어 가능. 3) 대용량 바이너리를 concat으로 반복 합치면 O(n²) 복사가 발생 — Buffer.concat(chunks, totalLength)로 한 번에 합치자. 4) ESP32와 MQTT 통신 시 little-endian float32 파싱 패턴이 자주 쓰인다.",
    "deepDive": "Buffer.from(string, 'utf8')과 Buffer.from(string, 'base64')의 차이는? Buffer.allocUnsafe가 보안 위험이 될 수 있는 상황을 설명하라 Node.js Streams에서 Buffer와 string 인코딩 문제를 어떻게 다루는가?",
    "tags": [
      "buffer",
      "binary",
      "uint8array",
      "typedarray",
      "encoding"
    ]
  },
  {
    "id": "nodejs-014",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "short-answer",
    "question": "Node.js crypto 모듈에서 bcrypt 대신 scrypt나 argon2를 사용하는 이유와 비밀번호 해싱 시 성능과 보안의 트레이드오프를 설명하라.",
    "answer": "bcrypt는 메모리 하드하지 않아 GPU 병렬 공격에 취약. scrypt(Node.js 내장)와 argon2는 메모리 집약적으로 설계되어 GPU/ASIC 공격을 어렵게 함. 단, 해싱 비용이 이벤트 루프와 libuv 스레드 풀을 점유하므로 동시 요청 수가 많으면 병목이 됨",
    "explanation": "비밀번호 해싱은 의도적으로 느리게 설계된다. bcrypt는 CPU 비용만 높이지만 메모리는 4KB 고정으로 GPU 병렬화가 가능하다. scrypt와 argon2는 메모리 사용량도 조절할 수 있어(N, r, p 파라미터) 메모리 하드 함수라 불린다. Node.js에 내장된 crypto.scrypt()를 사용하면 외부 의존성 없이 안전한 해싱이 가능하다.",
    "seniorTip": "실무 전략: 1) crypto.scrypt()는 libuv 스레드 풀을 사용하므로 동시 해싱 요청이 UV_THREADPOOL_SIZE를 초과하면 큐에 쌓임 — 회원가입/로그인 버스트 트래픽 시 응답 지연 주의. 2) Lambda에서 bcrypt/scrypt를 사용하면 cold start + 해싱 시간이 타임아웃을 유발할 수 있음. 3) JWT 기반 인증에서는 해싱이 로그인 시에만 발생하므로 상대적으로 영향 적음. 4) 세션 토큰, CSRF 토큰 등 랜덤값은 crypto.randomBytes()로 생성하고 절대 Math.random()을 쓰지 말 것.",
    "deepDive": "scrypt의 N, r, p 파라미터가 각각 무엇을 의미하는가? crypto.timingSafeEqual()은 왜 필요하고 어떤 공격을 막는가? Node.js Web Crypto API(globalThis.crypto)와 crypto 모듈의 차이는?",
    "tags": [
      "crypto",
      "security",
      "password-hashing",
      "scrypt",
      "performance"
    ]
  },
  {
    "id": "nodejs-015",
    "category": "nodejs",
    "difficulty": "mid",
    "type": "multiple-choice",
    "question": "ESM(ES Modules)과 CJS(CommonJS)의 차이 중 올바르지 않은 것은?",
    "options": [
      "ESM은 정적 분석이 가능하여 Tree-shaking에 유리하다",
      "CJS의 require()는 동기적으로 모듈을 로드하고, ESM의 import는 비동기적으로 로드된다",
      "ESM에서 CJS 모듈을 import하면 named export를 직접 구조분해할 수 있다",
      "ESM은 Top-level await를 지원한다"
    ],
    "answer": 2,
    "explanation": "ESM에서 CJS 모듈을 import할 때 named export를 직접 구조분해하는 것은 지원되지 않는다. CJS 모듈은 module.exports 전체를 default import로만 가져올 수 있다. 나머지 선택지는 모두 올바르다. ESM은 정적 바인딩으로 Tree-shaking에 유리하고, Top-level await를 지원하며, 로딩은 비동기(파싱은 동기)다.",
    "seniorTip": "실무에서 ESM/CJS 혼용 문제: 1) package.json에 \"type\": \"module\" 설정 시 .js 파일이 ESM으로 해석됨. CJS를 유지하려면 .cjs 확장자 사용. 2) TypeScript에서 tsconfig의 module을 ESNext/NodeNext로 설정하면 import 시 .js 확장자를 명시해야 함. 3) Jest는 기본적으로 CJS이므로 ESM 테스트를 위해 --experimental-vm-modules 또는 vitest 사용. 4) NestJS 프로젝트에서 ESM 마이그레이션은 고통스러울 수 있음 — 대부분의 NestJS 생태계가 CJS 기반. 5) tsup, esbuild는 CJS/ESM 동시 출력을 지원하여 라이브러리 배포 시 유용.",
    "deepDive": "Top-level await가 CJS에서 불가능한 이유는? ESM의 live binding이란 무엇이고 CJS와 어떻게 다른가? Node.js에서 .mjs 파일과 package.json의 exports 필드를 함께 사용하는 방법은?",
    "tags": [
      "esm",
      "commonjs",
      "modules",
      "tree-shaking",
      "typescript"
    ]
  },
  {
    "id": "nodejs-016",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "short-answer",
    "question": "Node.js 애플리케이션을 --inspect 플래그로 디버깅하는 방법과 메모리 누수를 힙 스냅샷(Heap Snapshot)으로 분석하는 과정을 설명하라.",
    "answer": "node --inspect(또는 --inspect-brk)로 실행 후 Chrome DevTools(chrome://inspect)에 연결. 힙 스냅샷을 두 시점에서 촬영하고 비교(Comparison 뷰)하여 증가한 객체를 추적",
    "explanation": "node --inspect 실행 시 WebSocket 디버그 서버가 9229 포트에 열린다. Chrome의 chrome://inspect에서 Remote Target으로 연결하면 Profiler, Memory 탭을 사용할 수 있다. 힙 스냅샷은 특정 시점의 메모리를 덤프하므로, 정상 상태와 누수 의심 상태의 스냅샷을 비교하면 새로 생성되어 해제되지 않은 객체를 식별할 수 있다.",
    "seniorTip": "실전 메모리 누수 분석 프로세스: 1) node --inspect app.js로 실행. 2) 트래픽을 일정 시간 흘리거나 특정 API를 반복 호출. 3) DevTools Memory 탭 → Take Heap Snapshot (스냅샷1). 4) 추가로 트래픽 흘리기. 5) Take Heap Snapshot (스냅샷2). 6) Comparison 뷰에서 #Delta(+) 높은 객체 확인 → Retainers 탭으로 참조 체인 추적. K8s에서 OOM Kill이 반복되면 kubectl exec -it로 --inspect를 포트포워딩해서 원격 프로파일링도 가능. clinic.js heapProfiler나 v8-profiler-node8도 좋은 대안.",
    "deepDive": "--inspect와 --inspect-brk의 차이는? Retainer(리테이너)란 무엇이고 누수 추적에서 어떻게 활용하는가? 운영 환경에서 힙 스냅샷을 안전하게 수집하는 방법은?",
    "tags": [
      "debugging",
      "heap-snapshot",
      "inspect",
      "memory-leak",
      "chrome-devtools"
    ]
  },
  {
    "id": "nodejs-017",
    "category": "nodejs",
    "difficulty": "mid",
    "type": "ox",
    "question": "process.env의 값은 항상 문자열(string) 타입이므로, 숫자나 불리언 환경변수는 타입 변환이 필요하다.",
    "answer": "O",
    "explanation": "OS 환경변수는 모두 문자열로 전달된다. PORT=3000을 설정해도 process.env.PORT는 '3000'(문자열)이므로 Number(process.env.PORT) 변환이 필요하다. ENABLE_CACHE=true도 'true'(문자열)이므로 process.env.ENABLE_CACHE === 'true'로 비교해야 한다. 이 점을 모르고 if (process.env.ENABLE_CACHE)를 쓰면 'false'도 truthy로 평가된다.",
    "seniorTip": "환경변수 관리 실무 패턴: 1) dotenv + Joi/Zod로 시작 시 환경변수 스키마 검증 → 필수 변수 누락이나 잘못된 타입을 서버 시작 시점에 조기 실패로 감지. 2) NestJS ConfigModule의 validationSchema 옵션 활용. 3) K8s에서는 ConfigMap(비민감)/Secret(민감)을 환경변수로 주입. 4) AWS Lambda에서는 환경변수를 핸들러 외부에서 한 번만 읽어 모듈 스코프 변수에 캐싱하면 warm 실행 시 재파싱 비용을 절약. 5) process.env를 테스트에서 직접 수정할 수 있으나 테스트 간 오염 주의 — beforeEach/afterEach에서 복원.",
    "deepDive": "dotenv가 process.env를 어떻게 채우는지 내부 동작을 설명하라 K8s Secret을 환경변수로 주입할 때와 볼륨으로 마운트할 때의 차이와 보안 고려사항은? 환경변수 대신 AWS Parameter Store나 Vault를 사용해야 하는 기준은?",
    "tags": [
      "process-env",
      "configuration",
      "dotenv",
      "type-coercion",
      "secrets"
    ]
  },
  {
    "id": "nodejs-018",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "multiple-choice",
    "question": "Child Process, Worker Threads, Cluster 모듈의 특징으로 올바른 것은?",
    "options": [
      "Worker Threads는 별도 프로세스를 생성하므로 메모리를 공유할 수 없다",
      "Child Process는 IPC를 통해 부모와 통신하며 독립 메모리 공간을 가지고, Worker Threads는 같은 프로세스 내 스레드로 SharedArrayBuffer로 메모리 공유가 가능하다",
      "Cluster는 Worker Threads의 확장판으로 내부적으로 같은 구현을 사용한다",
      "세 방식 모두 CPU 코어를 하나만 사용한다"
    ],
    "answer": 1,
    "explanation": "Child Process: fork()로 독립 프로세스 생성, 별도 메모리 공간, IPC(process.send/message)로 통신. Worker Threads: 같은 프로세스 내 스레드, SharedArrayBuffer와 Atomics로 메모리 공유 가능, MessageChannel로 통신. Cluster: 마스터가 워커 프로세스를 fork하고 소켓을 공유, HTTP 서버 부하 분산에 특화.",
    "seniorTip": "선택 기준: 1) CPU 집약적 계산 (이미지 처리, 암호화) → Worker Threads (메모리 공유로 데이터 복사 최소화). 2) 외부 바이너리 실행, 완전한 격리 필요 → Child Process (spawn/exec/fork). 3) 웹 서버 멀티코어 활용 → Cluster (단, K8s에서는 Pod replica로 대체). 4) SharedArrayBuffer + Atomics를 쓰면 뮤텍스 없이 lock-free 동시성이 가능하지만 레이스 컨디션 주의. 5) 실시간 IoT 데이터 처리에서 MQTT 메시지를 Worker Thread에서 파싱하고 메인 스레드는 WebSocket 브로드캐스트만 담당하는 패턴이 효과적.",
    "deepDive": "Worker Threads에서 SharedArrayBuffer를 안전하게 사용하려면 Atomics가 왜 필요한가? child_process.fork()와 spawn()의 차이는? Cluster 모드에서 여러 워커가 동일한 포트를 공유하는 원리는?",
    "tags": [
      "child-process",
      "worker-threads",
      "cluster",
      "concurrency",
      "ipc"
    ]
  },
  {
    "id": "nodejs-019",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "short-answer",
    "question": "Node.js의 프로토타입 오염(Prototype Pollution) 공격이 무엇이고, 어떻게 방어하는가? 실제 취약점 사례와 함께 설명하라.",
    "answer": "공격자가 __proto__ 또는 constructor.prototype을 통해 Object.prototype을 조작하여 모든 객체의 동작을 변경하는 공격. 입력값 검증(JSON Schema), Object.freeze(Object.prototype), 안전한 객체 병합 라이브러리 사용으로 방어",
    "explanation": "프로토타입 오염의 예: JSON.parse('{\"__proto__\":{\"admin\":true}}')를 lodash.merge() 같은 구형 deep merge 함수에 넣으면 Object.prototype.admin이 true가 되어 모든 객체에서 obj.admin이 true로 평가된다. lodash 4.17.15 이전 버전, jQuery, express-fileupload 등에서 실제 CVE가 보고된 바 있다.",
    "seniorTip": "방어 전략: 1) npm audit를 CI 파이프라인에 포함하여 알려진 취약점 자동 감지. 2) 외부 입력으로 객체 병합 시 Object.assign() 대신 __proto__를 무시하는 구현 사용 또는 JSON Schema로 입력 구조 제한. 3) Object.create(null)로 프로토타입 없는 순수 딕셔너리 객체 생성. 4) ReDoS(정규식 서비스 거부) 방어: 복잡한 정규식에 신뢰할 수 없는 입력을 넣지 말 것. safe-regex 패키지로 취약한 정규식 탐지. 5) 공급망 공격(supply chain): package-lock.json 커밋, npm audit, Dependabot 활성화, 패키지 출처 검증.",
    "deepDive": "Object.freeze(Object.prototype)가 프로토타입 오염을 방어하는 원리는? ReDoS(Regular Expression Denial of Service) 취약점이 이벤트 루프에 미치는 영향은? npm 공급망 공격(supply chain attack)을 방어하기 위한 실무적 전략 3가지는?",
    "tags": [
      "security",
      "prototype-pollution",
      "redos",
      "supply-chain",
      "npm-audit"
    ]
  },
  {
    "id": "nodejs-020",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "short-answer",
    "question": "clinic.js와 0x를 사용한 Node.js 성능 프로파일링 방법을 설명하고, 플레임 그래프에서 병목을 어떻게 식별하는가?",
    "answer": "clinic doctor로 이벤트 루프 지연/CPU/메모리를 종합 진단하고, clinic flame 또는 0x로 V8 CPU 샘플링 플레임 그래프를 생성. 플레임 그래프에서 너비가 넓고(실행 시간이 긴) 꼭대기에 가까운 프레임이 최적화 대상",
    "explanation": "플레임 그래프는 함수 호출 스택을 시각화한다. x축은 시간(CPU 샘플 수), y축은 호출 깊이다. 아래가 호출자, 위가 피호출자. 너비가 넓을수록 CPU를 많이 사용한다. '플레임'의 꼭대기 평평한 부분(plateau)이 실제 실행 시간을 소비하는 함수다.",
    "seniorTip": "실전 프로파일링 워크플로우: 1) clinic doctor -- node app.js로 종합 건강 진단. 이벤트 루프 지연, 메모리 증가, CPU 패턴을 한 눈에 확인. 2) 병목 의심 시 0x로 플레임 그래프 생성: 0x -- node app.js. 3) wrk나 autocannon으로 부하를 걸면서 프로파일링. 4) 플레임 그래프에서 빨간색/주황색 프레임 집중 확인. 5) 실무 발견 패턴: JSON.stringify()가 플레임 상위에 자주 등장 → 응답 직렬화 최적화 필요. 6) K8s 환경에서는 kubectl port-forward로 디버그 포트를 로컬로 터널링해 원격 프로파일링 가능.",
    "deepDive": "플레임 그래프에서 'idle' 또는 'epoll_wait'가 넓게 나타나는 것은 좋은 신호인가? clinic bubbles는 어떤 문제를 진단하는 데 특화되어 있는가? V8 CPU 프로파일러의 샘플링 방식이 프로파일링 결과에 어떤 편향을 일으킬 수 있는가?",
    "tags": [
      "profiling",
      "clinic-js",
      "flame-graph",
      "performance",
      "0x"
    ]
  },
  {
    "id": "nodejs-021",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "short-answer",
    "question": "AsyncLocalStorage를 사용한 요청 컨텍스트 전파(Request Context Propagation)란 무엇이고, 분산 추적(distributed tracing)에서 어떻게 활용하는가?",
    "answer": "AsyncLocalStorage는 비동기 호출 체인 전반에 걸쳐 컨텍스트(예: requestId, userId, traceId)를 자동으로 전파한다. 미들웨어에서 스토어를 초기화하면 해당 요청의 모든 async 콜백에서 동일한 컨텍스트에 접근 가능",
    "explanation": "기존에는 요청 컨텍스트를 함수 인자로 계속 넘겨야 했다. AsyncLocalStorage는 async_hooks 기반으로 비동기 경계를 넘어도 컨텍스트가 유지된다. Express 미들웨어에서 als.run({requestId: uuid()}, next)를 호출하면 이후 모든 비동기 작업(DB 쿼리, 외부 API 호출, 로거)에서 als.getStore().requestId에 접근할 수 있다.",
    "seniorTip": "실무 활용 패턴: 1) 로거에 requestId를 자동 삽입 — pino나 winston 인스턴스를 AsyncLocalStorage에서 가져와 모든 로그에 requestId가 자동으로 포함되게 설정. 2) OpenTelemetry의 Context는 내부적으로 AsyncLocalStorage를 사용하여 traceId/spanId를 전파 — Jaeger, DataDog에서 요청 전체 추적 가능. 3) NestJS에서 cls-hooked(Node 12 이하) 또는 @nestjs/cls 패키지가 이를 래핑하여 쉽게 사용. 4) Socket.IO 실시간 세션 컨텍스트 전파에도 활용 가능 — 소켓 연결별 컨텍스트 관리. 5) 성능 오버헤드는 미미하지만 async_hooks 자체의 미세한 비용은 존재함.",
    "deepDive": "AsyncLocalStorage와 구형 domain 모듈의 차이는 무엇인가? AsyncLocalStorage의 컨텍스트가 Worker Threads로 전파되지 않는 이유는? OpenTelemetry가 AsyncLocalStorage를 활용하는 방식을 설명하라",
    "tags": [
      "async-local-storage",
      "context-propagation",
      "tracing",
      "opentelemetry",
      "async-hooks"
    ]
  },
  {
    "id": "nodejs-022",
    "category": "nodejs",
    "difficulty": "mid",
    "type": "multiple-choice",
    "question": "Node.js의 LTS(Long Term Support) 전략에 대한 설명으로 올바른 것은?",
    "options": [
      "모든 Node.js 버전이 동일한 기간 동안 지원된다",
      "짝수 버전(18, 20, 22)이 LTS로 지정되며 30개월 동안 지원되고, 홀수 버전은 6개월 후 지원 종료된다",
      "LTS 버전에서만 새로운 기능이 추가된다",
      "홀수 버전이 LTS이고 짝수 버전은 최신 기능 실험용이다"
    ],
    "answer": 1,
    "explanation": "Node.js는 짝수 버전을 LTS로 지정한다. 짝수 버전은 출시 6개월 후 Active LTS 상태가 되어 18개월간 유지보수, 이후 12개월간 Maintenance 상태로 총 30개월 지원된다. 홀수 버전은 최신 기능을 테스트하는 Current 버전으로 6개월 후 지원 종료된다.",
    "seniorTip": "팀 Node.js 버전 관리 전략: 1) 프로덕션은 항상 Active LTS 버전 사용. 현재 시점(2026년 초) 기준 Node.js 22가 Active LTS. 2) nvm(Node Version Manager) 또는 fnm(Fast Node Manager)으로 로컬 버전 관리. .nvmrc 파일을 리포지토리에 커밋하여 팀 표준화. 3) Docker 이미지는 node:22-alpine 같이 LTS 메이저 버전을 고정하되 패치는 자동으로 받도록 구성. 4) node:22-alpine과 node:22-slim의 차이: alpine은 musl libc(네이티브 모듈 호환성 주의), slim은 glibc 기반. 5) 업그레이드 전 node:releases 확인 및 주요 변경사항(breaking changes) 검토.",
    "deepDive": "Node.js 메이저 버전 업그레이드 시 가장 먼저 확인해야 할 것들은? Alpine 이미지에서 네이티브 애드온(bcrypt, sharp 등)이 문제가 되는 이유는? fnm이 nvm보다 빠른 기술적 이유는?",
    "tags": [
      "lts",
      "version-management",
      "nvm",
      "docker",
      "node-releases"
    ]
  },
  {
    "id": "nodejs-023",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "short-answer",
    "question": "npm, yarn, pnpm의 차이를 설명하고, 모노레포(monorepo)에서 workspaces를 활용하는 방법과 각 패키지 매니저의 트레이드오프를 비교하라.",
    "answer": "npm은 표준이지만 느림. yarn은 병렬 설치와 PnP로 성능 개선. pnpm은 컨텐츠 주소 저장소(CAS)와 하드링크로 디스크 절약 및 가장 빠른 설치 속도 제공. workspaces로 모노레포 내 패키지 간 로컬 참조와 공통 의존성 호이스팅 관리",
    "explanation": "pnpm의 핵심 차이: 패키지를 ~/.pnpm-store에 한 번만 저장하고 각 프로젝트에는 하드링크를 생성하므로 디스크 사용량이 대폭 줄고 설치가 빠르다. 또한 npm/yarn처럼 의존성을 최상위로 평탄화(flatten)하지 않아 팬텀 의존성(phantom dependency) 문제를 방지한다. workspaces를 통해 모노레포의 packages/*를 연결하고 루트에서 모든 패키지를 관리한다.",
    "seniorTip": "모노레포 실무 패턴: 1) pnpm workspaces + Turborepo 조합이 현재 가장 권장됨 — Turborepo의 빌드 캐시와 병렬 실행으로 CI 시간 단축. 2) 공통 TypeScript 설정, ESLint 설정을 별도 패키지로 분리하여 extends. 3) pnpm의 --filter 플래그로 특정 패키지만 빌드/테스트: pnpm --filter @app/api build. 4) K8s 마이크로서비스를 모노레포로 관리 시 각 서비스별 Dockerfile에서 pnpm fetch + pnpm install --offline 패턴으로 Docker 레이어 캐시 최적화. 5) phantom dependency 허용이 필요하면 pnpm의 shamefully-hoist 옵션을 최후 수단으로 사용.",
    "deepDive": "pnpm의 팬텀 의존성(phantom dependency) 방지가 왜 중요한가? Turborepo의 빌드 캐시(Remote Cache)가 CI/CD에서 어떻게 작동하는가? yarn berry의 Plug'n'Play(PnP)가 node_modules를 제거하는 방식과 단점은?",
    "tags": [
      "npm",
      "yarn",
      "pnpm",
      "monorepo",
      "workspaces",
      "turborepo"
    ]
  },
  {
    "id": "nodejs-024",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "multiple-choice",
    "question": "TypeScript 컴파일 전략(tsc, swc, esbuild)에 대한 설명으로 올바르지 않은 것은?",
    "options": [
      "esbuild와 swc는 Go/Rust로 작성되어 tsc보다 수십~수백 배 빠른 트랜스파일 속도를 제공한다",
      "esbuild와 swc는 TypeScript의 타입 검사(type checking)를 tsc와 동일하게 수행한다",
      "ts-node는 개발 환경에서 TypeScript를 런타임에 직접 실행하지만 프로덕션에서는 권장되지 않는다",
      "swc-loader나 esbuild-loader를 webpack과 함께 사용하면 빌드 속도를 개선할 수 있다"
    ],
    "answer": 1,
    "explanation": "esbuild와 swc는 타입 정보를 제거(strip)하여 JavaScript로 변환하는 트랜스파일만 수행하며, TypeScript의 타입 검사는 전혀 하지 않는다. 타입 오류가 있어도 빌드가 성공한다. 타입 검사는 별도로 tsc --noEmit을 실행해야 한다. 이 분리가 빠른 빌드의 핵심이다.",
    "seniorTip": "실무 TypeScript 빌드 전략: 1) 개발 서버: ts-node-dev 또는 tsx (esbuild 기반) — 빠른 재시작. 2) CI 타입 체크: tsc --noEmit — 타입 오류만 검사, JS 출력 없음. 3) 프로덕션 빌드: swc(tsup) 또는 esbuild — 최고 속도의 트랜스파일. 4) NestJS + swc: nest build --watch에 swc를 적용하면 재빌드가 수 초 → 수백ms로 단축. 5) Lambda 배포 시 esbuild로 번들링하면 cold start 개선 (node_modules 포함 단일 파일, Tree-shaking 적용). 6) 타입 체크와 빌드를 분리하는 것이 모던 TypeScript 프로젝트의 표준.",
    "deepDive": "isolatedModules 옵션이 왜 swc/esbuild와 함께 사용할 때 권장되는가? esbuild가 TypeScript 타입을 단순히 제거하는 방식의 한계는 무엇인가? tsup이 라이브러리 배포용으로 많이 사용되는 이유는?",
    "tags": [
      "typescript",
      "tsc",
      "swc",
      "esbuild",
      "compilation",
      "build-performance"
    ]
  },
  {
    "id": "nodejs-025",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "short-answer",
    "question": "Node.js에서 AsyncLocalStorage를 활용한 멀티테넌트 요청 추적과 Sentry/OpenTelemetry 통합 시 고려해야 할 사항을 설명하라. 실시간 소켓 서버에서 요청 컨텍스트가 소실되는 상황을 어떻게 방지하는가?",
    "answer": "AsyncLocalStorage.run()으로 각 HTTP 요청/소켓 연결마다 독립 컨텍스트를 생성. OpenTelemetry의 Context API가 내부적으로 AsyncLocalStorage를 사용하므로 자동 전파됨. 소켓 서버에서는 connection 이벤트 핸들러에서 run()을 시작하여 해당 소켓의 모든 이벤트 핸들러에 컨텍스트가 유지되도록 구성",
    "explanation": "HTTP 서버와 달리 WebSocket/Socket.IO는 지속 연결이므로 연결마다 한 번 AsyncLocalStorage.run()을 호출하면 그 소켓의 생명주기 동안 컨텍스트가 유지된다. 그러나 Worker Threads나 외부 큐(Bull)에서 실행되는 작업은 원래 요청 컨텍스트를 잃으므로, traceId 등 추적에 필요한 값을 명시적으로 직렬화하여 전달해야 한다.",
    "seniorTip": "실시간 시스템 통합 패턴: 1) Socket.IO 서버에서 socket.data.traceId = opentelemetry.trace.getSpan()?.spanContext().traceId 패턴으로 소켓 레벨에서 traceId 보존. 2) Redis Pub/Sub로 이벤트를 전달할 때 메시지 페이로드에 traceId를 포함시켜 구독자가 스팬을 이어받을 수 있도록 함. 3) Sentry의 setUser(), setTag()를 AsyncLocalStorage 미들웨어에서 호출하면 모든 에러 리포트에 사용자/요청 정보가 자동 첨부됨. 4) BullMQ 잡에서 OpenTelemetry 컨텍스트를 전파하려면 잡 데이터에 W3C TraceContext 헤더(traceparent)를 포함시키고, 워커에서 propagation.extract()로 복원. 5) 멀티테넌트 환경에서 AsyncLocalStorage로 tenantId를 전파하면 DB 쿼리마다 tenantId를 인자로 넘길 필요가 없어 코드가 간결해짐.",
    "deepDive": "BullMQ Worker에서 OpenTelemetry span 컨텍스트를 복원하는 구체적인 방법은? AsyncLocalStorage의 enterWith()와 run()의 차이와 위험성은? 멀티테넌트 SaaS에서 AsyncLocalStorage로 DB 연결 풀을 테넌트별로 라우팅하는 패턴을 설명하라",
    "tags": [
      "async-local-storage",
      "opentelemetry",
      "sentry",
      "socket-io",
      "multi-tenant",
      "tracing"
    ]
  },
  {
    "id": "nodejs-026",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "short-answer",
    "question": "AsyncLocalStorage의 내부 동작 원리(async_hooks 기반)와 성능 오버헤드를 설명하고, 프로덕션에서 사용해도 안전한지 판단 근거를 제시하라.",
    "answer": "AsyncLocalStorage는 async_hooks의 init/before/after/destroy 훅으로 비동기 리소스 생성/소멸을 추적하여 컨텍스트를 전파한다. Node.js 16+에서 최적화되어 오버헤드가 약 8-10% 수준으로 줄었으며, OpenTelemetry/Datadog 등 주요 APM이 내부적으로 사용할 정도로 프로덕션에서 안전하다.",
    "explanation": "async_hooks는 Promise, setTimeout, I/O 콜백 등 모든 비동기 리소스의 생명주기를 추적한다. AsyncLocalStorage는 이를 활용하여 run() 시점의 store를 모든 자식 비동기 작업에 전파한다. Node.js 12에서는 async_hooks 오버헤드가 20-30%였지만, 16+에서 PromiseHook API 도입으로 Promise 추적이 최적화되었다.",
    "seniorTip": "성능 측정 포인트: 1) AsyncLocalStorage 활성화 전후로 autocannon 벤치마크를 돌려 RPS 차이를 측정. 2) 일반적으로 I/O 바운드 앱에서는 오버헤드가 거의 체감되지 않는다 — DB 쿼리 50ms에 비해 ALS 오버헤드 수 us는 무시할 수준. 3) CPU 집약적 코드에서 Promise를 대량 생성하면 오버헤드가 누적될 수 있다. 4) enterWith()는 run()과 달리 현재 컨텍스트를 영구적으로 변경하므로 예측하기 어려운 버그를 만들 수 있어 사용을 피하자. 5) 니콘내콘에서 RequestContext로 사용하는 패턴이 바로 ALS 기반이며, 179개 서비스에서 안정적으로 운영 중이다.",
    "deepDive": "왜 Node.js 12에서 async_hooks 성능이 나빴을까? → 모든 Promise 생성마다 init 훅이 동기적으로 호출되었기 때문이다. V8의 Promise는 마이크로태스크 큐에서 매우 빈번하게 생성되므로 오버헤드가 컸다. 그러면 Node.js 16+에서 어떻게 개선했나? → V8의 PromiseHook API를 사용하여 Promise 추적을 엔진 레벨에서 처리하고, AsyncLocalStorage만 활성화하면 불필요한 async_hooks 콜백을 건너뛰도록 최적화했다. 그러면 enterWith()가 왜 위험한가? → enterWith()는 run()처럼 콜백 스코프를 만들지 않고 현재 실행 컨텍스트의 store를 교체한다. 같은 async context에서 이후 실행되는 모든 코드가 영향을 받으므로, Express 미들웨어 체인에서 사용하면 다른 요청의 컨텍스트를 오염시킬 수 있다.",
    "tags": [
      "async-local-storage",
      "async-hooks",
      "performance",
      "context-propagation",
      "internals"
    ]
  },
  {
    "id": "nodejs-027",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "multiple-choice",
    "question": "Node.js에서 대용량 JSON(수 MB)을 JSON.parse()로 처리할 때 발생하는 문제와 대안으로 올바른 것은?",
    "options": [
      "JSON.parse()는 비동기이므로 이벤트 루프에 영향이 없다",
      "JSON.parse()는 동기적으로 실행되어 이벤트 루프를 블로킹하므로, 스트리밍 JSON 파서(jsonstream, stream-json)를 사용하거나 Worker Thread에서 파싱해야 한다",
      "JSON.parse()는 V8이 자동으로 청크 단위로 처리한다",
      "JSON.parse()의 성능 문제는 Node.js 20에서 완전히 해결되었다"
    ],
    "answer": 1,
    "explanation": "JSON.parse()는 완전히 동기적인 C++ 바인딩이다. 5MB JSON을 파싱하면 약 50-100ms 동안 이벤트 루프가 멈춘다. 이 시간 동안 모든 HTTP 요청, 타이머, I/O 콜백이 대기한다. 초당 수천 요청을 처리하는 서버에서 100ms 블로킹은 심각한 레이턴시 스파이크를 유발한다.",
    "seniorTip": "실무 대안: 1) stream-json — ReadableStream에서 JSON 토큰을 하나씩 파싱하여 이벤트 루프 블로킹 없이 처리. 2) Worker Thread — JSON.parse를 별도 스레드에서 실행하고 결과만 메인 스레드로 전달. 3) simdjson (simdjson-node) — SIMD 명령어를 활용한 고속 JSON 파서, 기본 JSON.parse보다 2-5배 빠름. 4) 애초에 대용량 JSON 응답을 줄이는 것이 가장 좋은 해결책 — 페이지네이션, GraphQL 필드 선택 등. 니콘내콘에서 외부 파트너 API 응답이 수 MB일 때 이 문제가 실제로 발생할 수 있다.",
    "deepDive": "왜 V8이 JSON.parse()를 비동기로 만들지 않았을까? → JSON.parse()는 ECMAScript 명세에서 동기 함수로 정의되어 있다. 비동기로 변경하면 모든 기존 코드가 깨진다. 그러면 JSON.stringify()도 같은 문제가 있나? → 맞다. 대용량 객체를 직렬화할 때도 이벤트 루프를 블로킹한다. NestJS의 응답 직렬화에서 Interceptor가 큰 객체를 JSON.stringify하면 같은 문제가 발생한다. 그러면 실제로 얼마나 큰 JSON이 문제가 되나? → 일반적으로 1MB 이상부터 체감된다. V8의 JSON 파서는 약 1GB/s 속도이므로, 1MB는 약 1ms, 10MB는 약 10ms, 100MB는 약 100ms가 걸린다. P99 레이턴시가 중요한 서비스에서는 1MB부터 스트리밍 파서를 고려하자.",
    "tags": [
      "json-parse",
      "event-loop-blocking",
      "streaming",
      "worker-threads",
      "performance"
    ]
  },
  {
    "id": "nodejs-028",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "short-answer",
    "question": "Node.js HTTP Agent의 keep-alive 설정이 마이크로서비스 간 통신 성능에 미치는 영향과, K8s 환경에서의 최적 설정을 설명하라.",
    "answer": "keep-alive를 활성화하면 매 요청마다 TCP 3-way handshake + TLS handshake를 반복하지 않아 지연이 크게 줄어든다. K8s에서는 Service DNS 기반 로드밸런싱과 keep-alive 연결의 상호작용을 고려해야 한다.",
    "explanation": "기본 HTTP Agent는 keep-alive가 false여서 매 요청마다 소켓을 새로 생성한다. TCP handshake(1 RTT) + TLS(2 RTT)를 합치면 요청 전에 약 3 RTT가 소비된다. keep-alive를 활성화하면 소켓을 재사용하여 이 오버헤드가 사라진다. Node.js 19+에서는 기본적으로 keep-alive가 활성화되었다.",
    "seniorTip": "K8s 환경 주의점: 1) K8s Service는 기본적으로 iptables/IPVS 기반 L4 로드밸런싱이다. keep-alive 연결이 특정 Pod에 고정되면 부하가 불균등하게 분산될 수 있다. 2) maxSockets를 적절히 제한하여 하나의 백엔드 Pod에 연결이 집중되지 않도록 한다. 3) keepAliveMsecs로 유휴 연결의 TCP keepalive 간격을 설정하여 중간 프록시(ALB, Envoy)의 idle timeout보다 짧게 유지. 4) Pod가 종료될 때 keep-alive 연결이 끊기므로 ECONNRESET 에러 핸들링 필수. 5) axios 사용 시 기본 httpAgent를 new http.Agent({ keepAlive: true, maxSockets: 50 })로 교체하자.",
    "deepDive": "왜 K8s Service 기반 로드밸런싱에서 keep-alive가 문제될 수 있나? → K8s Service는 연결 시작 시점에 Pod를 선택한다. keep-alive로 연결이 유지되면 해당 Pod로 모든 후속 요청이 간다. Pod 3개 중 하나에만 요청이 집중될 수 있다. 그러면 어떻게 해결하나? → 1) maxSockets를 Pod 수 이상으로 설정하여 여러 Pod에 연결을 분산. 2) maxFreeSockets로 유휴 연결 수를 제한하여 연결을 주기적으로 재생성. 3) 또는 gRPC처럼 L7 로드밸런싱(Envoy, Linkerd)을 사용하면 요청 단위로 분산된다. 그러면 Node.js 19+에서 keep-alive가 기본 활성화된 이유는? → 대부분의 HTTP 클라이언트 사용 패턴에서 keep-alive가 성능에 유리하고, 웹 브라우저는 이미 오래전부터 기본값이었기 때문이다.",
    "tags": [
      "http-agent",
      "keep-alive",
      "kubernetes",
      "connection-pooling",
      "microservices"
    ]
  },
  {
    "id": "nodejs-029",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "short-answer",
    "question": "AbortController/AbortSignal을 사용하여 Node.js에서 타임아웃을 관리하는 패턴과, 기존 setTimeout 기반 타임아웃의 문제점을 설명하라.",
    "answer": "AbortController는 fetch, fs, stream, child_process 등 Node.js 네이티브 API와 통합되어 작업을 실제로 취소할 수 있다. 기존 setTimeout 기반은 타임아웃 시 작업 자체는 계속 실행되며 결과만 무시하는 문제가 있다.",
    "explanation": "const controller = new AbortController(); setTimeout(() => controller.abort(), 5000); await fetch(url, { signal: controller.signal }). abort()가 호출되면 AbortError가 발생하고 TCP 연결이 실제로 종료된다. Promise.race([fetchPromise, timeoutPromise])는 느린 Promise가 백그라운드에서 계속 실행되어 리소스를 낭비한다.",
    "seniorTip": "Node.js 18+ 활용 패턴: 1) AbortSignal.timeout(5000) — 5초 후 자동 abort, AbortController 생성 없이 간결하게 사용. 2) fs.readFile(path, { signal }) — 파일 읽기 취소. 3) stream.pipeline(src, dest, { signal }) — 스트림 파이프라인 취소. 4) child_process.spawn(cmd, { signal }) — 자식 프로세스 종료. 5) 여러 작업을 하나의 AbortController로 묶어 동시 취소 가능: const controller = new AbortController(); Promise.all([fetch(url1, { signal }), fetch(url2, { signal })]). 에러 시 controller.abort()로 모든 요청을 한번에 취소. 니콘내콘에서 외부 파트너 API 호출 시 타임아웃을 AbortSignal로 관리하면 리소스 낭비를 방지할 수 있다.",
    "deepDive": "왜 Promise.race()를 타임아웃에 쓰면 안 되나? → Promise.race([fetch(url), sleep(5000)])에서 fetch가 5초 이상 걸리면 sleep이 먼저 resolve되어 타임아웃 처리를 하지만, fetch의 TCP 연결은 여전히 열려 있다. 응답이 결국 도착하면 메모리와 소켓이 낭비된다. 그러면 AbortController는 이 문제를 어떻게 해결하나? → abort() 호출 시 내부적으로 TCP 소켓의 destroy()를 호출하여 연결을 즉시 종료한다. 서버 측에서도 연결이 끊겼음을 인지하여 불필요한 작업을 중단할 수 있다. 그러면 AbortController를 지원하지 않는 라이브러리는? → axios는 v0.22+에서 signal 옵션을 지원한다. 구형 라이브러리는 AbortSignal.addEventListener('abort', () => req.destroy())로 수동 연결할 수 있다.",
    "tags": [
      "abort-controller",
      "abort-signal",
      "timeout",
      "resource-cleanup",
      "fetch"
    ]
  },
  {
    "id": "nodejs-030",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "multiple-choice",
    "question": "WeakRef와 FinalizationRegistry가 Node.js 메모리 관리에서 유용한 시나리오는?",
    "options": [
      "모든 객체 참조를 WeakRef로 바꾸면 메모리 사용량이 줄어든다",
      "캐시 구현에서 메모리 압박 시 GC가 자동으로 캐시 항목을 해제하게 하고, FinalizationRegistry로 해제된 항목을 Map에서 정리할 수 있다",
      "WeakRef는 Promise의 메모리 누수를 방지한다",
      "FinalizationRegistry로 DB 커넥션 풀을 관리할 수 있다"
    ],
    "answer": 1,
    "explanation": "WeakRef는 GC가 대상 객체를 수거하는 것을 막지 않는 약한 참조다. 캐시에서 WeakRef를 사용하면 메모리가 부족할 때 GC가 캐시된 객체를 자동으로 수거한다. FinalizationRegistry는 객체가 GC로 수거된 후 콜백을 호출하여 관련 리소스(Map의 키 등)를 정리할 수 있게 한다.",
    "seniorTip": "실무 활용과 주의점: 1) WeakRef 기반 캐시: const cache = new Map(); cache.set(key, new WeakRef(expensiveObj)); 조회 시 deref()가 undefined이면 캐시 미스. 2) FinalizationRegistry: GC 수거 후 Map에서 해당 키를 삭제하여 WeakRef 객체 자체의 누수를 방지. 3) 주의: GC 타이밍은 예측 불가능하므로 정확한 캐시 크기 제어는 불가능 — TTL 기반 캐시(lru-cache)가 대부분의 경우 더 적합. 4) FinalizationRegistry 콜백 실행 시점도 보장되지 않으므로 크리티컬한 리소스 정리(파일 핸들, DB 연결)에는 사용하면 안 된다. 명시적 close()를 사용하자. 5) WeakMap은 키가 GC되면 자동 삭제되지만, 키로 객체만 사용 가능.",
    "deepDive": "왜 WeakRef가 일반 캐시보다 나은 상황이 있을까? → 캐시에 저장된 객체의 크기가 크고 예측 불가능할 때, TTL이나 LRU 정책으로는 메모리 사용량을 정확히 제어할 수 없다. WeakRef를 사용하면 V8 GC가 메모리 압박 상황에서 자동으로 해제해주므로 OOM을 방지할 수 있다. 그러면 왜 실무에서 잘 안 쓰나? → GC 타이밍이 비결정적이라 캐시 히트율을 보장할 수 없다. 메모리가 여유 있을 때는 GC가 동작하지 않아 WeakRef 객체가 계속 살아있고, 메모리가 부족해지면 한꺼번에 수거되어 캐시 미스가 폭증할 수 있다. 그러면 어떤 경우에 정말 유용한가? → DOM 요소 참조(브라우저), 대용량 이미지/버퍼 캐시, 또는 메모리 제한이 엄격한 Edge/Lambda 환경에서 보조적으로 사용할 때 효과적이다.",
    "tags": [
      "weakref",
      "finalization-registry",
      "memory-management",
      "cache",
      "garbage-collection"
    ]
  },
  {
    "id": "nodejs-031",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "short-answer",
    "question": "Node.js Diagnostics Channel(node:diagnostics_channel)이 무엇이고, 기존 로깅/모니터링 방식 대비 어떤 장점이 있는지 설명하라.",
    "answer": "Diagnostics Channel은 Node.js 내장 Pub/Sub 메시지 채널로, 코어 모듈(http, net, worker_threads)과 서드파티 라이브러리가 진단 이벤트를 발행하면 APM/로거가 구독하는 방식이다. 코드 수정 없이 런타임에 관측성(observability)을 추가할 수 있다.",
    "explanation": "기존 방식은 monkey-patching(http.request를 감싸기)이나 코드 내 로거 호출이 필요했다. Diagnostics Channel은 표준화된 이벤트 채널을 제공하여, OpenTelemetry나 Datadog이 http.client.request, net.client.socket 같은 채널을 구독하면 HTTP 요청의 시작/종료/에러를 자동으로 트레이싱할 수 있다.",
    "seniorTip": "활용 패턴: 1) const dc = require('node:diagnostics_channel'); const ch = dc.channel('my-app:query'); ch.publish({ sql, duration }). 2) Undici(Node.js 내장 HTTP 클라이언트)는 이미 Diagnostics Channel을 발행하므로 APM이 자동 계측한다. 3) Express/Fastify도 Diagnostics Channel 지원을 추가하고 있어, 미들웨어 체인을 monkey-patch하지 않고도 요청 추적이 가능해진다. 4) 성능 오버헤드: 구독자가 없으면 publish()는 거의 no-op이므로 프로덕션 코드에 항상 포함해도 안전하다. 5) 니콘내콘의 로거/APM 통합을 Diagnostics Channel 기반으로 전환하면 코드 침투성을 줄일 수 있다.",
    "deepDive": "왜 monkey-patching보다 Diagnostics Channel이 더 좋은가? → monkey-patching(require('http').request = wrappedRequest)은 모듈 로드 순서에 의존하고, 여러 APM이 동시에 패치하면 충돌한다. 또한 ESM 환경에서는 모듈 변수가 read-only라 패치가 불가능할 수 있다. 그러면 Diagnostics Channel은 어떻게 이 문제를 해결하나? → 발행자와 구독자가 완전히 분리된 Pub/Sub 모델이므로, 여러 APM이 동시에 구독해도 충돌이 없다. Node.js 코어에서 직접 발행하므로 모듈 로드 순서와 무관하다. 그러면 커스텀 채널을 만들어 비즈니스 이벤트를 발행하면? → 가능하다. 예: dc.channel('ncnc:coupon:publish').publish({ couponId, publisherType, duration })로 쿠폰 발급 이벤트를 발행하고, Datadog Custom Metric으로 수집하면 발행사별 성능을 실시간 모니터링할 수 있다.",
    "tags": [
      "diagnostics-channel",
      "observability",
      "apm",
      "tracing",
      "monkey-patching"
    ]
  },
  {
    "id": "nodejs-032",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "ox",
    "question": "process.exit()를 호출하면 진행 중인 비동기 작업(HTTP 요청, DB 쿼리, 파일 쓰기)이 안전하게 완료된 후 프로세스가 종료된다.",
    "answer": "X",
    "explanation": "process.exit()는 이벤트 루프를 즉시 중단한다. 진행 중인 I/O 작업, 열린 소켓, 버퍼링된 로그가 모두 버려진다. SQS 메시지를 처리 중이었다면 메시지가 ACK되지 않아 재전달되고, DB 트랜잭션이 커밋되지 않아 데이터 불일치가 발생할 수 있다.",
    "seniorTip": "안전한 종료 패턴: 1) SIGTERM 핸들러에서 HTTP 서버를 닫고(server.close()), 진행 중인 요청 완료를 기다린 후, DB 연결을 종료하고, 그때 process.exit(0) 호출. 2) NestJS에서는 app.enableShutdownHooks()가 이를 자동화한다. 3) 절대 process.exit()를 에러 핸들러에서 즉시 호출하지 말 것 — 로그가 버퍼링되어 있으면 에러 로그가 유실된다. 4) process.exitCode = 1; 을 설정하면 이벤트 루프가 자연스럽게 비었을 때 해당 코드로 종료된다. 5) K8s에서 SIGTERM 후 terminationGracePeriodSeconds(기본 30초) 이내에 종료되지 않으면 SIGKILL이 전송되므로, graceful shutdown 로직이 이 시간 안에 완료되어야 한다.",
    "deepDive": "왜 process.exit() 대신 process.exitCode를 사용해야 하나? → process.exitCode = 1을 설정하면 Node.js가 이벤트 루프에 대기 중인 모든 작업(타이머, I/O, Promise)을 완료한 후 해당 exit code로 종료한다. 이벤트 루프가 자연스럽게 비어야 종료되므로 데이터 유실이 없다. 그러면 이벤트 루프가 비지 않으면 영원히 안 죽지 않나? → 맞다. 그래서 graceful shutdown 로직에서 모든 서버/연결을 close()하여 이벤트 루프를 비워야 한다. 만약 10초 안에 비지 않으면 강제로 process.exit()를 호출하는 타임아웃을 두는 것이 일반적이다. 그러면 uncaughtException에서는? → process.on('uncaughtException')에서는 프로세스 상태가 불확실하므로, 에러를 로깅하고 짧은 타임아웃 후 process.exit(1)로 강제 종료하는 것이 권장된다. 이때 로그 플러시를 위해 logger.end()의 콜백을 기다린 후 종료하자.",
    "tags": [
      "process-exit",
      "graceful-shutdown",
      "sigterm",
      "event-loop",
      "kubernetes"
    ]
  },
  {
    "id": "nodejs-033",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "short-answer",
    "question": "Node.js HTTP 서버에서 TCP backlog와 net.Server.listen()의 backlog 파라미터가 고부하 상황에서 미치는 영향을 설명하라.",
    "answer": "backlog는 TCP 3-way handshake가 완료되었지만 accept()되지 않은 연결의 큐 크기다. Node.js 기본값은 511이며, 이벤트 루프가 블로킹되어 accept()가 지연되면 이 큐가 가득 차 새 연결이 거부(ECONNREFUSED)된다.",
    "explanation": "클라이언트가 TCP SYN을 보내면 OS 커널이 handshake를 완료하고 backlog 큐에 넣는다. Node.js의 이벤트 루프가 connection 이벤트를 처리하면 큐에서 꺼낸다. 이벤트 루프가 CPU 작업으로 100ms 블로킹되면 그 동안 accept()되지 않아 큐가 빠르게 찬다.",
    "seniorTip": "프로덕션 튜닝: 1) server.listen(3000, '0.0.0.0', 1024) — backlog를 1024로 늘려 버스트 트래픽 수용. 2) OS 레벨: net.core.somaxconn = 4096 (리눅스 커널 파라미터). Node.js backlog가 somaxconn보다 크면 somaxconn으로 잘린다. 3) K8s Pod에서 sysctl 설정이 필요하면 securityContext.sysctls로 지정. 4) backlog 큐가 가득 차면 OS가 SYN을 무시하거나 RST를 보내는데, 이는 클라이언트에서 ECONNREFUSED로 나타난다. 5) 니콘내콘 같은 이벤트 트래픽(쿠폰 할인 이벤트 시 트래픽 폭증)에서 backlog 튜닝이 첫 번째 방어선이 될 수 있다.",
    "deepDive": "왜 backlog 기본값이 511인가? → 역사적으로 BSD 소켓의 기본값에서 유래했다. 현대 서버에서는 대부분 부족한 값이다. 그러면 backlog를 무한정 늘리면 되나? → 아니다. backlog가 크면 연결은 수락하지만 실제 처리가 지연되어 클라이언트가 연결은 되었는데 응답이 안 오는 상태가 된다. 빠르게 거부(fail-fast)하는 것이 오히려 나을 수 있다. 그러면 Node.js에서 backlog 문제를 어떻게 감지하나? → ss -tlnp 명령으로 Recv-Q(현재 큐 크기)와 Send-Q(backlog 설정값)를 비교한다. Recv-Q가 Send-Q에 가까우면 큐가 가득 차고 있다는 신호다. Datadog에서 system.net.tcp.listen_overflows 메트릭으로 모니터링할 수 있다.",
    "tags": [
      "tcp-backlog",
      "listen",
      "high-load",
      "connection-queue",
      "kernel-tuning"
    ]
  },
  {
    "id": "nodejs-034",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "multiple-choice",
    "question": "crypto.timingSafeEqual()을 사용해야 하는 이유와, 일반 === 비교로 토큰을 검증할 때의 보안 취약점은?",
    "options": [
      "=== 비교가 더 느리기 때문에 timingSafeEqual이 성능상 유리하다",
      "=== 비교는 첫 번째 불일치 바이트에서 즉시 반환하므로 응답 시간 차이로 올바른 토큰을 한 바이트씩 추론하는 타이밍 공격(timing attack)에 취약하다",
      "=== 비교는 Buffer를 비교할 수 없기 때문이다",
      "timingSafeEqual은 암호화된 비교를 수행하기 때문이다"
    ],
    "answer": 1,
    "explanation": "=== 또는 Buffer.compare()는 첫 번째 불일치에서 short-circuit 반환한다. 토큰의 첫 바이트가 맞으면 두 번째 바이트 비교로 넘어가 약간 더 오래 걸린다. 공격자는 수천 번의 요청으로 응답 시간을 통계 분석하여 올바른 토큰을 한 바이트씩 추론할 수 있다.",
    "seniorTip": "사용 패턴: 1) HMAC 검증, API 키 비교, webhook signature 검증 시 반드시 timingSafeEqual 사용. 2) 두 버퍼의 길이가 다르면 에러가 발생하므로, 먼저 길이를 고정(해싱 후 비교)하거나 길이가 다르면 바로 false를 반환. 3) 니콘내콘에서 외부 파트너 웹훅의 서명 검증(HMAC-SHA256)이나 API 키 비교에서 이 함수를 사용해야 한다. 4) 문자열 비교 시 Buffer.from(a).equals(Buffer.from(b)) 대신 timingSafeEqual(Buffer.from(a), Buffer.from(b))을 사용하자.",
    "deepDive": "타이밍 공격이 실제로 가능한가? → 로컬 네트워크에서는 나노초 단위 차이도 측정 가능하고, 인터넷을 통해서도 수만 번의 요청으로 통계적 분석이 가능하다는 연구가 있다. 특히 같은 데이터센터 내(같은 K8s 클러스터)에서는 네트워크 지터가 매우 적어 공격이 더 쉬워진다. 그러면 timingSafeEqual은 어떻게 상수 시간을 보장하나? → 모든 바이트를 XOR하고 결과를 OR로 누적하여 항상 전체 버퍼를 순회한다. 어떤 위치에서 불일치가 발생하든 실행 시간이 동일하다. 그러면 bcrypt.compare()도 타이밍 공격에 안전한가? → bcrypt의 compare 함수는 내부적으로 상수 시간 비교를 사용하므로 안전하다. 하지만 직접 해시를 비교할 때는 반드시 timingSafeEqual을 사용하자.",
    "tags": [
      "timing-attack",
      "crypto",
      "security",
      "constant-time",
      "hmac"
    ]
  },
  {
    "id": "nodejs-035",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "short-answer",
    "question": "Node.js에서 --max-old-space-size 설정과 K8s Pod의 memory limits를 어떻게 정렬(align)해야 하는지 설명하라. 잘못 설정하면 어떤 문제가 발생하는가?",
    "answer": "--max-old-space-size는 V8 힙의 Old Generation 최대 크기를 설정한다. K8s memory limits보다 크게 설정하면 V8이 GC 대신 힙을 계속 늘리다가 K8s OOM Killer에 의해 프로세스가 강제 종료(SIGKILL)된다.",
    "explanation": "V8의 기본 Old Space는 약 1.5GB(64비트)다. K8s Pod의 memory limits가 512MB인데 max-old-space-size가 기본값이면, V8이 1.5GB까지 힙을 확장하려다 K8s가 OOM Kill한다. 반대로 max-old-space-size를 너무 작게 설정하면 빈번한 GC로 성능이 저하된다.",
    "seniorTip": "권장 공식: max-old-space-size = K8s memory limits * 0.75. 나머지 25%는 V8 New Space, 네이티브 메모리(Buffer, C++ 애드온), 스택 등에 필요하다. 예: limits.memory=1Gi → --max-old-space-size=768. 실무 체크리스트: 1) Dockerfile의 CMD에 --max-old-space-size 플래그 추가. 2) K8s requests와 limits를 같게 설정(Guaranteed QoS)하여 OOM Kill 우선순위를 낮춤. 3) Datadog에서 process.memoryUsage().heapUsed와 Pod 메모리 사용량을 동시에 모니터링. 4) heapUsed가 max-old-space-size의 90%를 넘으면 알람 설정. 5) 니콘내콘의 140+ ECR repos에서 각 서비스의 메모리 프로필이 다르므로 일괄 설정이 아닌 서비스별 튜닝이 필요하다.",
    "deepDive": "왜 K8s OOM Kill이 graceful shutdown보다 위험한가? → OOM Kill은 SIGKILL을 보내므로 프로세스가 어떤 정리 작업도 수행할 수 없다. SIGTERM 핸들러가 실행되지 않아 진행 중인 트랜잭션이 롤백되지 않고, 로그도 플러시되지 않는다. 그러면 V8이 자동으로 GC하면 되지 않나? → V8의 GC는 max-old-space-size에 도달하기 전에 Minor/Major GC를 실행한다. 하지만 GC는 V8 힙만 관리하고, Buffer.alloc()이나 네이티브 모듈이 사용하는 메모리는 V8 힙 밖(RSS)에 있다. 따라서 V8 힙은 여유 있는데 전체 RSS가 limits를 초과하여 OOM Kill될 수 있다. 그러면 어떻게 진단하나? → process.memoryUsage()의 rss(전체), heapUsed(V8 힙), external(C++ 객체), arrayBuffers(Buffer)를 분리하여 모니터링하면 어느 영역이 메모리를 소비하는지 파악할 수 있다.",
    "tags": [
      "max-old-space-size",
      "kubernetes",
      "oom-kill",
      "memory-tuning",
      "v8"
    ]
  },
  {
    "id": "nodejs-036",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "ox",
    "question": "for await...of로 Node.js Readable Stream을 소비할 때, 스트림의 배압(backpressure)이 자동으로 관리되므로 메모리 문제가 발생하지 않는다.",
    "answer": "X",
    "explanation": "for await...of는 기본적으로 배압을 지원하지만, 루프 내부에서 비동기 작업(예: DB INSERT)이 느리면 문제가 발생할 수 있다. 스트림은 데이터를 빠르게 읽어오는데 처리가 느리면 내부 버퍼(highWaterMark)에 데이터가 축적되어 메모리가 증가한다. 특히 객체 모드 스트림에서 highWaterMark의 기본값은 16개 객체인데, 각 객체가 크면 메모리 사용량이 예상보다 커질 수 있다.",
    "seniorTip": "안전한 스트림 소비 패턴: 1) pipeline() 함수 사용이 가장 안전 — 에러 처리와 배압을 자동 관리. 2) for await...of 사용 시 루프 내 비동기 작업을 배치로 묶어 처리(100건씩 bulk insert). 3) Transform 스트림을 사용하여 처리 속도를 제어. 4) 스트림의 'drain' 이벤트를 활용한 수동 배압 제어. 5) highWaterMark를 조절하여 메모리 사용량과 처리량 간의 균형을 맞춤. 니콘내콘에서 대용량 CSV 가져오기나 SQS 배치 메시지 처리 시 이 패턴이 중요하다.",
    "deepDive": "왜 for await...of에서 배압 문제가 발생할 수 있나? → for await...of는 내부적으로 stream[Symbol.asyncIterator]()를 사용한다. 각 iteration에서 next()가 호출되면 스트림의 read()가 실행된다. 하지만 스트림의 readable 이벤트는 highWaterMark만큼 데이터를 미리 버퍼링하므로, 소비 속도가 느리면 이 버퍼가 계속 쌓인다. 그러면 pipeline()은 어떻게 다른가? → pipeline()은 Writable의 write() 반환값(false)을 확인하여 Readable을 자동으로 pause/resume한다. 쓰기가 느리면 읽기를 멈추므로 메모리가 일정하게 유지된다. 그러면 for await...of를 안전하게 쓰려면? → 루프 내에서 Writable에 write()하고 drain을 기다리는 패턴을 사용하거나, 단순히 pipeline()으로 교체하는 것이 가장 안전하다.",
    "tags": [
      "for-await-of",
      "streams",
      "backpressure",
      "memory",
      "pipeline"
    ]
  },
  {
    "id": "nodejs-037",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "short-answer",
    "question": "Node.js는 싱글 스레드이지만 MySQL 드라이버(mysql2)는 커넥션 풀 내부에서 어떻게 동시에 여러 쿼리를 실행하는지 설명하라.",
    "answer": "mysql2의 각 커넥션은 별도의 TCP 소켓이다. Node.js의 이벤트 루프가 비동기 I/O(epoll/kqueue)로 여러 소켓을 동시에 감시하므로, 싱글 스레드에서도 10개의 커넥션에서 10개의 쿼리를 '동시에' 실행할 수 있다. 실제 CPU 작업은 MySQL 서버의 스레드 풀에서 병렬로 처리된다.",
    "explanation": "Node.js가 pool.query()를 호출하면: 1) 풀에서 유휴 커넥션(TCP 소켓)을 가져온다. 2) SQL을 소켓에 쓴다(비동기). 3) MySQL 서버가 쿼리를 실행하는 동안 이벤트 루프는 다른 작업을 처리한다. 4) MySQL이 응답을 보내면 소켓의 data 이벤트가 발생하여 콜백이 실행된다. 핵심: 쿼리 실행 자체는 MySQL 서버(멀티스레드)에서, I/O 대기는 Node.js 이벤트 루프에서 담당한다.",
    "seniorTip": "커넥션 풀 크기 결정: 1) 공식: poolSize = (CPU 코어 수 * 2) + 유효 스핀들 수 (PostgreSQL 가이드라인이지만 MySQL에도 적용 가능). 2) 일반적으로 Pod당 10-20개가 적절. 너무 많으면 MySQL 서버의 max_connections를 초과. 3) K8s에서 Pod 10개 * 풀 20개 = 200개 커넥션. MySQL의 max_connections(기본 151)을 초과할 수 있다. 4) TypeORM의 poolSize 설정과 MySQL 서버의 max_connections를 함께 관리. 5) 커넥션 풀이 가득 차면 query()가 큐에 대기하다가 connectionLimit 초과 시 에러 발생. acquireTimeout 설정으로 대기 시간 제한.",
    "deepDive": "왜 커넥션 풀 크기를 무한정 늘리면 안 되나? → MySQL 서버의 각 커넥션은 약 10MB의 메모리를 소비한다(thread stack + sort buffer + join buffer 등). 1000개 커넥션이면 10GB가 커넥션에만 사용되어 InnoDB buffer pool에 할당할 메모리가 줄어든다. 또한 컨텍스트 스위칭 비용이 증가하여 오히려 성능이 저하된다. 그러면 K8s 스케일링 시 어떻게 관리하나? → HPA로 Pod가 자동 확장되면 전체 커넥션 수가 예측 불가능하게 증가한다. RDS Proxy나 ProxySQL 같은 커넥션 프록시를 사용하면 수백 개의 앱 커넥션을 수십 개의 MySQL 커넥션으로 멀티플렉싱할 수 있다. 그러면 Node.js에서 커넥션 풀 고갈을 모니터링하는 방법은? → mysql2의 pool.on('acquire'), pool.on('release'), pool.on('enqueue') 이벤트로 풀 상태를 실시간 추적하고, enqueue 이벤트가 빈번하면 풀 크기를 늘리거나 쿼리 최적화가 필요하다는 신호다.",
    "tags": [
      "connection-pool",
      "mysql",
      "event-loop",
      "concurrency",
      "kubernetes"
    ]
  },
  {
    "id": "nodejs-038",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "multiple-choice",
    "question": "Node.js의 패키지 해석(Package Resolution) 알고리즘에서 node_modules 호이스팅(hoisting)이 일으키는 문제는?",
    "options": [
      "패키지 설치 속도가 느려진다",
      "package.json에 선언하지 않은 의존성을 사용할 수 있는 유령 의존성(phantom dependency) 문제가 발생하여, 다른 환경에서 설치 시 모듈을 찾을 수 없는 오류가 발생한다",
      "TypeScript 타입 정의가 충돌한다",
      "ESM 모듈을 사용할 수 없게 된다"
    ],
    "answer": 1,
    "explanation": "npm/yarn은 중복을 줄이기 위해 의존성을 최상위 node_modules로 끌어올린다(hoisting). 라이브러리 A가 lodash에 의존하면 lodash가 최상위로 올라가고, 내 코드에서 lodash를 package.json에 선언하지 않아도 require('lodash')가 작동한다. 하지만 다른 환경에서 A가 lodash를 더 이상 사용하지 않거나 버전이 달라지면 코드가 깨진다.",
    "seniorTip": "해결 방법: 1) pnpm — 심볼릭 링크 기반으로 각 패키지가 자신의 의존성만 접근 가능. phantom dependency를 원천 차단. 2) yarn berry PnP — node_modules를 완전히 제거하고 .pnp.cjs로 의존성을 관리. 3) eslint-plugin-import의 no-extraneous-dependencies 규칙으로 선언되지 않은 의존성 import를 빌드 타임에 감지. 4) 니콘내콘이 pnpm workspace + Turborepo를 사용하는 이유 중 하나가 바로 이 문제 방지다. 5) CI에서 pnpm install --frozen-lockfile로 lockfile과 정확히 일치하는 설치를 강제하면 환경 간 차이를 방지할 수 있다.",
    "deepDive": "왜 npm은 호이스팅을 하게 되었나? → npm v2까지는 의존성을 중첩 구조(A/node_modules/B/node_modules/C)로 설치하여 같은 패키지가 수십 번 중복 설치되고 경로가 Windows PATH_MAX를 초과하는 문제가 있었다. npm v3에서 flat node_modules로 전환하여 중복을 줄였지만, 부작용으로 phantom dependency가 생겼다. 그러면 pnpm은 어떻게 해결하나? → pnpm은 모든 패키지를 전역 store에 한 번만 저장하고, 각 프로젝트의 node_modules에는 직접 의존성만 심볼릭 링크로 연결한다. A가 lodash에 의존하면 A의 node_modules/lodash는 존재하지만, 내 프로젝트의 node_modules/lodash는 내가 선언하지 않으면 존재하지 않는다. 그러면 pnpm의 단점은? → 심볼릭 링크를 지원하지 않는 도구(React Native의 Metro bundler 등)에서 문제가 될 수 있다. shamefully-hoist 옵션으로 기존 npm처럼 동작하게 할 수 있지만, 이는 pnpm의 핵심 장점을 포기하는 것이다.",
    "tags": [
      "package-resolution",
      "hoisting",
      "phantom-dependency",
      "pnpm",
      "node-modules"
    ]
  },
  {
    "id": "nodejs-039",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "short-answer",
    "question": "pnpm strict 모드가 유령 의존성(phantom dependency) 버그를 어떻게 방지하는지, 그리고 Turborepo 모노레포에서 pnpm workspace를 함께 사용할 때의 이점을 설명하라.",
    "answer": "pnpm은 심볼릭 링크 기반 node_modules 구조로 package.json에 명시된 의존성만 접근 가능하게 한다. 모노레포에서 workspace:* 프로토콜로 로컬 패키지를 참조하면 pnpm이 자동으로 심볼릭 링크를 생성하여 빌드 없이 변경 사항이 즉시 반영된다.",
    "explanation": "npm/yarn의 flat node_modules에서는 의존성의 의존성까지 최상위에 노출되어 선언하지 않은 패키지를 사용할 수 있다(phantom dependency). CI/CD나 다른 개발자 환경에서 설치 순서가 달라지면 해당 패키지가 없어 빌드가 실패한다. pnpm은 각 패키지가 자신의 의존성만 볼 수 있는 격리된 node_modules를 만든다.",
    "seniorTip": "모노레포 실무 패턴: 1) pnpm-workspace.yaml에 packages: ['apps/*', 'packages/*']를 설정하여 Turborepo의 앱/패키지 구조와 일치시킨다. 2) workspace:* 프로토콜: \"@ncnc/common-domain\": \"workspace:*\"로 로컬 패키지를 참조하면 npm publish 시 자동으로 실제 버전 번호로 치환된다. 3) pnpm --filter @ncnc/api-server build로 특정 패키지와 그 의존성만 빌드. 4) pnpm fetch + pnpm install --offline 패턴으로 Docker 레이어 캐시를 최적화하면 CI 빌드 속도가 크게 개선된다. 5) 니콘내콘의 140+ 서비스에서 의존성 일관성을 유지하는 핵심이 pnpm의 strict 모드다.",
    "deepDive": "왜 phantom dependency가 프로덕션 장애로 이어질 수 있나? → 개발 환경에서 A 패키지가 lodash@4.17.21을 의존하고, 이것이 호이스팅되어 내 코드에서 lodash를 사용했다. 6개월 후 A가 lodash를 제거하면, npm install 시 lodash가 설치되지 않아 프로덕션에서 'Cannot find module lodash' 에러가 발생한다. 그러면 pnpm에서 기존 프로젝트를 마이그레이션하면? → pnpm import로 기존 lockfile을 변환하고, pnpm install을 실행하면 phantom dependency에 의존하던 코드에서 즉시 에러가 발생한다. 이 에러들을 수정하는 것이 마이그레이션의 핵심이다. 그러면 CI에서 phantom dependency를 자동으로 감지하는 방법은? → pnpm 자체가 감지해주지만, 추가로 depcheck 도구로 package.json에 선언되었지만 사용하지 않는 의존성(unused dependency)이나 사용하지만 선언하지 않은 의존성(missing dependency)을 CI에서 자동 검사할 수 있다.",
    "tags": [
      "pnpm",
      "phantom-dependency",
      "strict-mode",
      "monorepo",
      "workspace"
    ]
  },
  {
    "id": "nodejs-040",
    "category": "nodejs",
    "difficulty": "senior",
    "type": "short-answer",
    "question": "Node.js에서 zlib 모듈을 사용한 스트리밍 gzip 압축의 구현 패턴과, API 응답 압축 시 고려해야 할 성능/호환성 trade-off를 설명하라.",
    "answer": "zlib.createGzip()을 Transform 스트림으로 사용하여 응답을 청크 단위로 압축한다. CPU 사용량이 증가하지만 네트워크 전송량이 70-90% 줄어든다. Accept-Encoding 헤더를 확인하여 클라이언트가 gzip을 지원할 때만 압축해야 한다.",
    "explanation": "NestJS/Express에서 compression 미들웨어를 사용하면 자동으로 처리된다. 동작: 1) 요청의 Accept-Encoding: gzip 확인. 2) 응답 본문을 zlib.createGzip() 스트림으로 파이핑. 3) Content-Encoding: gzip 헤더 설정. 4) Transfer-Encoding: chunked로 스트리밍 전송.",
    "seniorTip": "프로덕션 최적화: 1) compression의 threshold 옵션 — 1KB 미만 응답은 압축하지 않음(압축 오버헤드가 절감보다 큼). 2) zlib의 level 옵션 — Z_BEST_SPEED(1)부터 Z_BEST_COMPRESSION(9)까지. 기본값 6이 대부분 적절. 3) 이미 압축된 응답(이미지, 동영상)은 압축 제외 — filter 옵션 사용. 4) K8s 환경에서 ALB/Nginx가 이미 압축을 처리한다면 애플리케이션에서 중복 압축하면 CPU만 낭비. 5) Brotli(zlib.createBrotliCompress())는 gzip보다 20-30% 더 높은 압축률을 제공하지만 CPU 사용량이 더 높다. CDN에서 정적 자산에는 Brotli, 동적 API 응답에는 gzip이 일반적. 6) zlib 작업은 libuv 스레드 풀을 사용하므로 UV_THREADPOOL_SIZE를 고려해야 한다.",
    "deepDive": "왜 Nginx/ALB에서 압축하는 것이 Node.js에서 하는 것보다 나을 수 있나? → Nginx의 gzip 모듈은 C로 구현되어 Node.js의 zlib보다 효율적이고, 정적 파일의 사전 압축(gzip_static)을 지원하여 매 요청마다 압축하지 않아도 된다. 또한 Node.js의 이벤트 루프에서 CPU를 빼앗지 않는다. 그러면 Node.js에서 압축해야 하는 경우는? → Nginx/ALB가 없는 직접 노출 환경이거나, 응답을 스트리밍으로 생성하면서 동시에 압축해야 하는 경우(CSV 엑스포트 등). 그러면 Brotli와 gzip을 동시에 지원하는 방법은? → Accept-Encoding 헤더에서 br과 gzip 중 우선순위가 높은 것을 선택한다. 보통 br > gzip > deflate 순서. express-static-gzip 같은 미들웨어가 이를 자동으로 처리한다.",
    "tags": [
      "zlib",
      "gzip",
      "compression",
      "streaming",
      "performance"
    ]
  }
]